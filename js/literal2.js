const minNum = Number.EPSILON; /** 「1」と「1より大きい最小の値」の差 */
console.log("minNum "+ minNum);

const largestNum = Number.MAX_SAFE_INTEGER; /** 表現できる最大の整数 */
console.log("largestNum "+largestNum);

const maxNum = Number.MAX_VALUE; /** 表現できる最大の数値 */
console.log("maxNum "+maxNum);

const smallestNum = Number.MIN_SAFE_INTEGER; /** 表現できる最小の整数 */
console.log("smallestNum "+smallestNum);

const minNums = Number.MIN_VALUE; /** 表現できる最小の数値 */
console.log("minNums "+minNums);

const nInf = Number.NEGATIVE_INFINITY;
console.log("nInf "+nInf);

const inf = Number.POSITIVE_INFINITY;
console.log("inf "+inf);

const notNum = Number.NaN;
console.log("notNum "+notNum);
